/**
 * @since 2020-02-26 03:57
 * @author vivaxy
 */
import tokenize from '../tokenize';

test('', function () {
  const stateMachine = {};
  const code = ``;
  const tokens = tokenize(stateMachine, code);
  const expected = [];
  expect(tokens).toBe(expected);
})
